<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingStart="@dimen/activity_horizontal_margin"
    android:layout_marginStart="@dimen/list_horizonal_matgin"
    android:layout_marginEnd="@dimen/list_horizonal_matgin"
    android:paddingEnd="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context=".MainActivity$PlaceholderFragment"
    >
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/widget_select"
        android:text="@string/button_choose_day"
        android:contentDescription="@string/choose_day"
        android:visibility="gone"
        android:layout_gravity="center"
        android:layout_marginBottom="5dp"
        />
    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id = "@+id/scores_list"
        android:divider="@android:color/transparent"
        android:dividerHeight="15sp"
        android:contentDescription="@string/score_list"
        />
</LinearLayout>
